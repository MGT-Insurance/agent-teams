
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# Playwright MCP browser artifacts (snapshots/screenshots) — the tester drops these
.playwright-mcp/

# Eval harness runtime artifacts (manifest.json / result.json per run)
eval/runs/*
!eval/runs/.gitkeep
