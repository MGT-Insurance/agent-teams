// Stub: proves @agent-teams/shared resolves from web/.
// Track B (frontend shell) will replace this with real React app code.
import type { InboxItem, DrillInDetail } from "@agent-teams/shared";

const _unused: InboxItem = {
  initiativeId: "",
  title: "",
  kind: "human-gate",
  question: "",
  worktree: "",
  prUrl: null,
};

const _unused2: Pick<DrillInDetail, "id" | "title"> = {
  id: "",
  title: "",
};
