// Track D owns this file. Contract: default-export a React component.
// Props: none — read live data via useSnapshotContext() from SnapshotContext.
// Receives: initiatives + inbox from snapshot context (no per-view fetching needed).
//
// Replace this stub with the real Inbox view.
export default function InboxView() {
  return <div className="view-placeholder">Inbox — Track D coming</div>;
}
