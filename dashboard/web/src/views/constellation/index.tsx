// Track C owns this file. Contract: default-export a React component.
// Props: none — read live data via useSnapshotContext() from SnapshotContext.
// Navigation: clicking a node should call useNavigate() from react-router-dom
//   and push "/initiative/:id" to drill into an initiative.
//
// Replace this stub with the real Constellation view.
export default function ConstellationView() {
  return <div className="view-placeholder">Constellation — Track C coming</div>;
}
