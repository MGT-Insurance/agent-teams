// Stub: proves @agent-teams/shared resolves from server/.
// Track A (backend) will replace this with real server code.
import type { SnapshotEvent, InitiativeNode } from "@agent-teams/shared";

const _unused: SnapshotEvent = {
  initiatives: [] as InitiativeNode[],
  inbox: [],
  ts: 0,
};
