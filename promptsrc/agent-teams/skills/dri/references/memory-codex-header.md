# Memory mechanics — three-tier model

The core routing rule lives inline in the DRI skill: never write harness memory files; route transferable role/process knowledge through `ateam learn`, user preferences through `ateam learn user`, and repository-wide project facts through project `bd remember`. This reference owns the shared tier mechanics.
